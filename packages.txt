# System packages required for Playwright Chromium browser
# These are needed for Chromium to run properly in Linux environments
# 
# For Docker/local deployments: Install with:
#   sudo apt update && sudo apt install -y $(cat packages.txt)
#
# Note: Streamlit Cloud doesn't support packages.txt directly.
# If deploying to Streamlit Cloud, system dependencies should be available
# in the base image. If you encounter missing library errors, contact
# Streamlit Cloud support.

# NSPR (Netscape Portable Runtime) - required by Chromium
libnspr4
libnss3

# Additional Chromium dependencies
libatk-bridge2.0-0
libatk1.0-0
libatspi2.0-0
libcups2
libdbus-1-3
libdrm2
libgbm1
libgtk-3-0
libxkbcommon0
libxcomposite1
libxdamage1
libxfixes3
libxrandr2
libxss1
libasound2
